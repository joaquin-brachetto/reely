PORT=3000

# Opción A: conexión por variables sueltas (desarrollo local con docker-compose)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=movieapp
DB_USER=postgres
DB_PASSWORD=postgres123

# Opción B: conexión por URL (hosting tipo Render/Railway/Neon). Si está definida, tiene prioridad.
# DATABASE_URL=postgresql://usuario:password@host:5432/dbname

JWT_SECRET=un_secreto_largo_y_aleatorio
JWT_EXPIRES_IN=7d

# Gmail con contraseña de aplicación (https://myaccount.google.com/apppasswords)
EMAIL_USER=tu_email@gmail.com
EMAIL_PASS=tu_app_password

APP_NAME=Reely
FRONTEND_URL=http://localhost:5173

MAX_LOGIN_ATTEMPTS=20
MAX_CODE_ATTEMPTS=5
LOCKOUT_MINUTES=15
CODE_EXPIRES_MINUTES=15
